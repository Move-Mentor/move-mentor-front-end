import React from 'react';
import { render, screen } from '@testing-library/react';
import CatSits from './CatSits';

describe('CatSits', () => {
  test('renders', async () => {
    expect(true).toBeTruthy();
  });
});
